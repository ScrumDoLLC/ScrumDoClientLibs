This library requires REST::Client CPAN module. Installation steps will vary 
depending on the platform being used. Given below are the steps to install it 
on an OSX platform. These should work on Linux system too. 

$ sudo perl -MCPAN -e 'shell'
#Now run "install REST::Client" on the cpan tool console. 
cpan[5]> install REST::Client



